import { OtrsBoldDirective } from './otrs-bold.directive';

describe('OtrsBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new OtrsBoldDirective();
    expect(directive).toBeTruthy();
  });
});
